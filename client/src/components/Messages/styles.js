import { makeStyles } from '@material-ui/core/styles';

export default makeStyles( () => ({
  Container: {
    padding: '2% 0',
    overflow: 'auto',
  },
  Typing: {
    fontStyle: 'italic',
    fontSize: '20px',
  }
}));
